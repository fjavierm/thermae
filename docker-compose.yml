version: '3.3'

services:
  database:
    container_name: redditclone-postgres
    image: "postgres" # use latest official postgres version
    env_file:
      - database.env # configure postgres
    volumes:
      - postgres:/data/postgres
    ports:
      - 5432:5432

volumes:
  postgres: